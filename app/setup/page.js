"use client";import {useState} from "react";import {useRouter} from "next/navigation";import CompanyBrand from "@/components/CompanyBrand";
export default function Setup(){const r=useRouter(),[f,setF]=useState({username:"",name:"",password:""}),[e,setE]=useState("");async function go(x){x.preventDefault();const res=await fetch("/api/setup",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(f)});const d=await res.json();if(res.ok)r.replace("/");else setE(d.error)}return <main className="loginPage"><form className="loginCard" onSubmit={go}><CompanyBrand/><h1>First Admin Setup</h1><p>Create the first Super Admin for this database.</p>{e&&<div className="notice error">{e}</div>}{[["username","Username"],["name","Display Name"],["password","Password"]].map(([k,l])=><div className="field" key={k} style={{marginTop:10}}><label>{l}</label><input type={k==="password"?"password":"text"} value={f[k]} onChange={x=>setF({...f,[k]:x.target.value})}/></div>)}<br/><button className="btn btnPrimary" style={{width:"100%"}}>CREATE SUPER ADMIN</button></form></main>}
