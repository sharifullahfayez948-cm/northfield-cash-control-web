self.addEventListener("push",event=>{let data={};try{data=event.data?event.data.json():{}}catch{data={body:event.data?.text()||"New Northfield activity."}}event.waitUntil(self.registration.showNotification(data.title||"Northfield Cash Control",{body:data.body||"New activity requires your attention.",icon:"/northfield-logo.png",badge:"/app-icon.svg",tag:data.tag||"northfield-alert",renotify:false,vibrate:[160,80,160],data:{url:data.url||"/"}}))});
self.addEventListener("notificationclick",event=>{event.notification.close();const url=new URL(event.notification.data?.url||"/",self.location.origin).href;event.waitUntil(clients.matchAll({type:"window",includeUncontrolled:true}).then(windows=>{for(const client of windows){if(client.url.startsWith(self.location.origin)){client.navigate(url);return client.focus()}}return clients.openWindow(url)}))});
