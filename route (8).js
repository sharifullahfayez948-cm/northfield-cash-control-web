import { requireUser } from "@/lib/auth";import { daySnapshot } from "@/lib/business";import { query } from "@/lib/db";import { ok,fail } from "@/lib/api";
export async function GET(req){try{const u=await requireUser();const date=new URL(req.url).searchParams.get("date")||new Date().toISOString().slice(0,10);return ok(await daySnapshot(date,u.id));}catch(e){return fail(e.message,500)}}
export async function POST(req){try{const u=await requireUser();const b=await req.json();const snap=await daySnapshot(b.date,u.id);const actual=Number(b.actual);const variance=actual-snap.expected;await query(`update cash_days set expected_closing=$1,actual_cash=$2,variance=$3,status='FINALIZED',finalized_by=$4,finalized_at=now(),manager_note=$5,updated_at=now() where business_date=$6`,[snap.expected,actual,variance,u.id,b.note||"",b.date]);return ok({expected:snap.expected,variance});}catch(e){return fail(e.message,500)}}
